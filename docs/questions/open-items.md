# Open Items

These are now resolved from your answers.

1. DOGSpace hours are Monday to Friday, 13:00 to 18:00.
2. `DOGSpace`, `Stammtisch`, `Hundecafe` and workshops share one common framework.
3. The `5er-Karte` is still priced at 280 EUR.
4. Workshops and seminars run on demand.
5. `Coaching mit Hund` should get its own public page.
6. `Mantrailing-Training` remains planned.
7. `about.html` redirects to `Ueber mich`; `informationen.html` redirects to `Impressum`.

No blocking questions remain for the rebuild baseline.
